<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <!-- Console appender with structured format -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg %X{trace_id:--} %X{span_id:--}%n</pattern>
        </encoder>
    </appender>
    
    <!-- Fallback console appender with pattern layout -->
    <appender name="CONSOLE_FALLBACK" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- OpenTelemetry appender -->
    <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <includeInstrumentationScopeInfo>true</includeInstrumentationScopeInfo>
        <captureExperimentalAttributes>true</captureExperimentalAttributes>
        <captureCodeAttributes>true</captureCodeAttributes>
        <captureMarkerAttribute>true</captureMarkerAttribute>
        <captureKeyValuePairAttributes>true</captureKeyValuePairAttributes>
        <captureLoggerContext>true</captureLoggerContext>
        <captureMdcAttributes>*</captureMdcAttributes>
    </appender>
    
    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE_FALLBACK"/>
        <appender-ref ref="OTEL"/>
    </root>
    
    <!-- Application specific loggers -->
    <logger name="de.jmale" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE_FALLBACK"/>
        <appender-ref ref="OTEL"/>
    </logger>
    
    <!-- OpenTelemetry related loggers -->
    <logger name="io.opentelemetry" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE_FALLBACK"/>
    </logger>
    
    <!-- Reduce noise from third-party libraries -->
    <logger name="com.google" level="WARN"/>
    
</configuration>
